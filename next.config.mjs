/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'lasvcwigpukpiyyphadu.supabase.co',
            },
        ],
    },
};

export default nextConfig;
